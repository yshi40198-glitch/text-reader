# 薇阅文字朗读工具 v3.0 · 使用说明书

> 一句话记住：**打开文件 → 选朗读方式 → 听。** 其他功能都是让这件事更方便的。

---

## 一、第一次使用（3 步上手）

1. 解压「文字朗读工具v3.0.zip」。
2. 双击文件夹里的「创建桌面快捷方式.bat」，桌面出现金色羽毛图标。
3. 以后双击图标就能打开软件。

打开后，点「打开文件」选一个文档（PDF / Word / txt / EPUB / MOBI / FB2 / 网页都行），
然后点「从头朗读」就开始了。

---

## 二、朗读怎么控制（最常用）

| 按钮 | 作用 |
|------|------|
| 从头朗读 | 从文档开头读到结尾 |
| 从光标朗读 | 先点一下文字（出现竖线），从那里读到结尾 |
| 朗读选中 | 鼠标划选一段文字再点，只读选中的部分 |
| 暂停 / 继续 | 听到一半暂停，再点继续 |
| 停止 | 结束朗读 |
| 打标记 / 从标记朗读 | 听到关键处打标记，下次从这里继续 |

右键文字还有快捷菜单：从这里朗读、朗读选中、剪切/复制/粘贴/删除/全选。

**快捷键**：Ctrl+O 打开文件，Ctrl+P 暂停/继续，Esc 停止。

---

## 三、听感调节

### 倍速（0.5x ~ 3x）
- 点「快选」里的 0.5 / 1 / 1.5 / 2 / 3 直接切换。
- 朗读中切换，会从当前段落按新速度重新读，马上能听出差别。
- 0.5x~2x 用语音合成（音调自然），2x~3x 再叠加播放加速。

### 音量（0% ~ 100%）
- 右侧「音量」滑杆，拖动即时生效，旁边显示百分比。
- 夜晚听书调低不吵人。

### 语音来源与声线
- **AI 网络语音（推荐）**：自然逼真，需联网，60+ 种声线可选（晓晓、云希、Aria 等）。
- **本地系统语音**：离线可用，声线数量取决于 Windows 自带的语音包。
- 断网时 AI 语音自动改用本地语音，不会中断报错。

### 定时关闭
- 下拉选 15 / 30 / 60 / 90 分钟，到点自动停读，旁边有倒计时。
- 适合睡前听书。选「关闭」取消。

---

## 四、不怕丢进度（v3.0 重点）

### 自动记住阅读位置
- 读到一半关掉软件，下次打开会问「继续读吗？」，点「是」直接接着读。
- 停止、退出时也会保存；整本读完后自动清除记录。

### 打标记
- 听到关键处点「打标记」，之后点「从标记朗读」随时继续。

### 自定义发音词典
- 点工具栏「发音词典」，添加「原词 = 读法」，例如：iPhone = 爱疯、薇阅 = 薇 阅。
- 多音字、人名、品牌都能调；朗读、跟读、导出音频全部按你的词典发音。
- 词典保存在软件目录 weiyue_config.json 里，换电脑可备份这个文件。

### 大字号模式
- 工具栏右侧「大字号」按钮，一键放大正文，再点恢复。老人用更清楚。

---

## 五、书库（本地 + 云端）

点「打开文件」旁边的「书库」按钮，窗口顶部切换「本地书库」和「云端书库」。

### 本地书库
- 选一个放电子书的文件夹（Calibre 书库、自己的小说文件夹都行）。
- 薇阅自动扫描，按书名、作者、格式列出，双击即读；记住上次用的文件夹。
- 支持 EPUB / PDF / Word / TXT / MOBI / FB2 / 网页。
- **删除与回收站（v3.0）**：选中书点「删除本书」→ 书移入回收站，书单里马上消失；
  回收站窗口里可以「还原」或「彻底删除」，放心管理，不怕误删。

### 云端书库（连接你自己的服务器，可选）
- 薇阅默认**不连接任何服务器**，完全本地使用，隐私安全。
- 第一次使用：点「云端书库」→ 「服务器设置」→ 填服务器地址
  （例如 https://你的服务器.com），确定后自动加载书单。
- 双击书直接下载朗读；右上角「刷新」随时重新拉取书单。
- **删除书要输入删除密钥**（只有你知道，第一次输一次，软件记住）。
- **上架新书只能在服务器文件里操作**（把书放进服务器 library 文件夹），
  软件端无法上架，别人也删不了你的书。

### 云端书库更新提醒（v3.0）
- 打开软件时自动对比服务器书单，发现新书会弹窗提示「云端书库有新内容」。
- 适合 AI / 自动化脚本往书库里放书后，你一打开就能知道。

---

## 六、早间新闻（AI 自动送新闻）

### 方式一：本地文件夹（最简单）
- 软件目录下有个「早间新闻」文件夹（首次运行自动创建）。
- 把当天的新闻（txt / md）放进去，打开软件就提示「要现在听吗？」，点「是」朗读。

### 方式二：云端自动拉取（v3.0，配合 AI / 自动化脚本）
- 配置了服务器地址后，薇阅启动时自动去服务器的「早间新闻」文件夹
  拉取当天文件（早间新闻-日期.txt），有就提示朗读。
- 服务器端可安装自动任务，每天定时生成当天新闻，全自动。

### 托盘随时听
- 右下角托盘菜单有「早间新闻」，随时打开最新一期。

---

## 七、学英语 / 学习类功能

### 文本翻译
- 点「翻译」打开翻译面板：粘贴文字 → 「翻译」→ 中英互译。
- 长文本自动分段；译文可「朗读译文」，可暂停/停止。

### 录音跟读（复读机模式）
- 先打开一篇文章，点「跟读」。
- 听原句（可暂停/继续/停止，复读 1/3/5 次或循环，间隔 0.5/1/2 秒）。
- 点「录音」跟着读，再点停止；「回放」对比自己的发音。
- 单次录音上限 3 分钟；录音只在内存中，关窗口即释放。

### 图片识别（OCR）
- 点「图片识别」，选一张图片（书页照片、截图、扫描件）。
- Windows 自带引擎离线识别文字并立即朗读。适合纸质书页。

### 读链接
- 粘贴新闻/文章网址，薇阅抓取正文朗读。
- 有反爬保护的网站抓不到时，会提示你复制正文粘贴朗读。

### 智能过滤（自动开启）
- 朗读时自动跳过页码、页眉页脚，听感更干净。

---

## 八、导出音频

- 点「导出音频」：AI 语音存 MP3，本地语音存 WAV。
- 长文档生成需要几分钟，失败会自动重试。

---

## 九、其他常用

- **中英双语界面**：右上角「EN / 中」一键切换，全部文字即时变化。
- **系统托盘**：点关闭按钮会最小化到托盘（朗读不中断），点图标呼出；
  托盘菜单：打开界面 / 暂停 / 停止 / 早间新闻 / 退出。
- **配置文件**：软件目录的 weiyue_config.json 保存你的设置
  （书库目录、发音词典、服务器地址、阅读进度），备份它等于备份设置。

---

## 十、常见问题

**打开 .doc（旧版 Word）读不了？**
本机装了 Word 才能直接读；没装就提示你另存为 .docx 再打开。

**PDF 是扫描件/图片？**
没有文字层的 PDF 需要先用「图片识别」OCR，或直接 OCR 图片。

**AI 声线用不了？**
检查网络；断网会自动改用本地语音继续读，不会中断。

**云端书库连不上？**
确认「服务器设置」填了带 https:// 的完整地址，且服务器在线。

**删除书提示失败？**
确认删除密钥输入正确；密钥在服务器上单独保存，改过要用新的。

**手机网页版怎么用？**
手机浏览器打开 https://你的服务器/tts.html，书库和手机版共用同一个服务器书库。
